## 1 
It will print the value 3


## 2 
It will print the value 150

## 3 
This will print the value 150

## 4
The function will return a array with values [50, 100, 150]. It will do this because all of the values got pushed into the return array in the for loop

## 5 
Return an error as i is now limited to the scope of the for loop and cannot be accessed

## 6
Return an error as discounted price is now limited to the for loop, so it will not be declared outside the scope of the for loop


## 7 
Will return the calculated value of 150, this variable was defined before the for loop and updated so it will have an accessible value

## 8
The function will return a array with values [50, 100, 150]. It will do this because all of the values got pushed into the return array in the for loop
Similar reason to 7, was declared earlier

## 9 
We will hit an error as i is not defined in the scope outside of the for loop

## 10 
Length will return the length of prices which is 3


## 11
This will return the array [50,100,150] because although the array is constant it is still mutable as long as it is not reassigned

## 12 
A.  student.name
B.  student["Grad Year"]
C.  student.greeting
D.  student["Favorite Teacher"].name
E.  student.courseLoad[0]


## 13
A. "32" the integer 2 is mapped to "2" so this is just a concatenation
B. Integer val 1, "3" is converted to int and then subtracted by 2
C. 3 + null is the same as 3 + nothing so it is still 3
D. "3null" null is converted to string and concated
E. 4, true is treated as 1 
F. 0 , false is 0 and null is 0 so 0 + 0
G. "3undefined" 3 + the string undefined is a concatenation
H. Nan math done on an undefined value leads to a Nan error

## 14
A. True, "2" is converted to an integer
B. False both are strings so there is alphabetical style comparison and 2 is greater than 1
C. True, the type is converted and then compared
D. False also checks that the types are equal which is false
E. False true maps to 1
F. True, any non zero number is true, the boolean conversion makes this true


## 15 
"===" does the same things as regular "==" except it also checks for strict type equivalency and does not allow for type conversion when checking


## 17
This will return [2,4,6]

It will call the modify array function first and create newArr then for every item in the array parameter it will call dosomething and the value of the current index and dosomething will multiply that value by 2 and push it into the new array so [1,2,3] will be turned into [2,4,6]


## 19
The output is 1 4 3 2